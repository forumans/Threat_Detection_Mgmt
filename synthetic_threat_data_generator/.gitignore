.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Generated benchmark releases (large binary/data output, not source)
datasets/

# Downloaded Piper voice models (large binaries, fetched via
# `python -m piper.download_voices`, not source -- see .env.example)
configs/voices/
